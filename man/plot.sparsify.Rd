% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/L1Clustering.R
\name{plot.sparsify}
\alias{plot.sparsify}
\title{Diagnostic plots for a clustered sparsify result}
\usage{
\method{plot}{sparsify}(x, type = c("grouping", "chain"), ...)
}
\arguments{
\item{x}{A clustered (grouping) \link{sparsify} object.}

\item{type}{One of \code{"grouping"} (recovered clusters, default) or \code{"chain"}
(the per-parameter scored cluster path).}

\item{...}{Ignored.}
}
\value{
A ggplot object.
}
\description{
Visualises a clustered (grouping) \link{sparsify} result. \code{type = "grouping"}
(default) draws, per candidate parameter, each subject's deviation mode
\eqn{\hat\eta} with the recovered clusters coloured and their shared centroids
drawn as a line: the recovered group structure at a glance. \code{type = "chain"}
draws the per-parameter cluster-path score chain (marginal \eqn{-2\log L}
against the number of clusters \eqn{G}), with its marginal-optimal grouping
highlighted, so the Occam trade-off is explicit; under the default joint
selection the final grouping (the "grouping" panel) may refine this
per-parameter one.
}
\seealso{
\link{sparsify}
}
