% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nlmeNormal.R
\name{emObjfn}
\alias{emObjfn}
\title{Build a quadrature-based NLME marginal-likelihood objective function}
\usage{
emObjfn(obj, control = list())
}
\arguments{
\item{obj}{An \code{objfn} of the form
\code{normL2(data, g*x*p, errmodel = err) + constraintL2(mu = 0, Omega = om)}.
The prediction function, data, error model, and omega spec are recovered
from it automatically.}

\item{control}{Named list with \code{level} (Smolyak depth, default 4),
\code{cores} (default 1), and \code{pruneTol} (node weight-pruning threshold,
default \code{Inf} = off).}
}
\value{
A callable of class \code{c("emobjfn", "objfn", "fn")}. Calling it on
\code{pars} returns an \link{objlist} with an \code{emDiag} attribute carrying
quadrature diagnostics.
}
\description{
Returns a callable \code{em(pars)} that integrates out the per-subject random
effects using a sparse-grid Gauss-Hermite rule. The ECM E-step refreshes
the integration nodes between outer iterations via
\code{attr(em, "rebuildQuadrature")}. Most users want \link{EM}, which builds
the right \code{em} internally and runs a solver; use \code{emObjfn} directly only
if you need to evaluate the objective by hand.
}
\details{
For the Laplace approximation of the marginal likelihood, use \link{EM}
with \code{method = "focei"}.
}
\examples{
\dontrun{
em <- emObjfn(obj, control = list(level = 5L))
attr(em, "rebuildQuadrature")(init)  # E-step: build the grid at `init`
em(init)                             # marginal objlist (+ emDiag attribute)
}
}
\seealso{
\link{EM}, \link{omega}
}
