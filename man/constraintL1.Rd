% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/penaltyClass.R
\name{constraintL1}
\alias{constraintL1}
\title{L1 / Laplace regularisation constraint over subject-level deviations}
\usage{
constraintL1(penalty, attr.name = "prior_l1", condition = NULL)
}
\arguments{
\item{penalty}{A \code{penaltySpec} (with subject expansion) from \link{penaltyL1},
possibly combined with \code{+}.}

\item{attr.name}{Name of the numeric attribute holding the penalty value.
Default \code{"prior_l1"} (distinct from constraintL2's \code{"prior"}, so the two
accumulate independently under \code{+}).}

\item{condition}{Optional condition (default \code{NULL}, condition-unspecific).}
}
\value{
An \code{objfn} carrying \code{attr(., "penaltySpec")}.
}
\description{
The L1 counterpart of \link{constraintL2} with an \code{Omega}. Given a \link{penaltyL1}
specification it contributes the penalty
\eqn{\lambda \sum_t |(D\eta)_t|}, where the terms \eqn{(D\eta)_t} are the
element-wise deviations toward a target (\code{lasso}/\code{fused}) or the pairwise
between-subject differences (\code{clustered}), and \eqn{\lambda} is the single
regularisation strength named by the spec. Stamps the \code{penaltySpec} on the
returned objfn so a composed objective
(\code{normL2(...) + constraintL1(...)}) self-describes for \link{EM} (see
\code{.nlmeReconstruct}).

The Hessian contribution of the L1 term is zero (its second derivative
vanishes away from the kinks); the conditional-mode solve is handled by
\link{trustL1} inside \link{EM}, not by feeding this term to \link{trust}.
}
\seealso{
\link{penaltyL1}, \link{EM}, \link{sparsify}, \link{constraintL2}
}
