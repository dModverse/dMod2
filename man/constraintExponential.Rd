% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/objClass.R
\name{constraintExponential}
\alias{constraintExponential}
\title{Soft exponential constraint on positive parameters}
\usage{
constraintExponential(scale, attr.name = "prior", condition = NULL)
}
\arguments{
\item{scale}{Named numeric vector of scale parameters, not rates. Its names
select the constrained parameters.}

\item{attr.name}{Character. Name of the attribute storing the constraint value.}

\item{condition}{Optional character vector of conditions.}
}
\value{
Object of class \code{objfn}.
}
\description{
Soft exponential constraint on positive parameters
}
\details{
Computes the exponential prior on the \verb{-2 log} scale,
\deqn{2p/s + 2\log s.}
It is the gamma prior at shape 1 and pulls a parameter towards 0 with a
constant force, which makes it the smooth one-sided counterpart of an L1
penalty at \code{mu = 0}. The value is \code{Inf} for a negative parameter.
}
\examples{
prior <- constraintExponential(scale = c(k1 = 3))
prior(pars = c(k1 = 5))$value
}
\seealso{
\link{constraintGamma}, \link{constraintL1}
}
