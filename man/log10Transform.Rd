% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eqnClass.R
\name{log10Transform}
\alias{log10Transform}
\title{Transform an ODE to log10 coordinates}
\usage{
log10Transform(f, suffix = "_l10", simplify = TRUE)
}
\arguments{
\item{f}{\code{eqnvec} (or named character vector) holding the right-hand sides.}

\item{suffix}{appended to each state name.}

\item{simplify}{cancel the division symbolically with sympy. \code{FALSE}, or an
unavailable sympy, leaves the plain quotient, which is equivalent but wordy.}
}
\value{
An \code{eqnvec} over the renamed states.
}
\description{
Reads \code{f} as the right-hand side of \eqn{\dot x = f(x)} and returns the same
system in \eqn{x_{l10} = \log_{10} x}. By the chain rule
}
\details{
\deqn{\dot x_{l10} = \frac{f(x)}{\log(10)\, x}, \qquad x = 10^{x_{l10}}}

so each right-hand side is divided by its own state and every remaining state
replaced by \verb{10^} of the transformed one. The division is cancelled
symbolically first, which is what makes the result readable: \code{x = "-k*x"}
becomes \code{x_l10 = "-k/log(10)"}, not a ratio that happens to simplify at
runtime.

Integrating in these coordinates cannot produce a negative state, since a power
of ten is positive for every real exponent -- positivity is the geometry of the
chart rather than a constraint checked afterwards. Where a trajectory would have
crossed \eqn{x = 0}, the transformed variable escapes to \code{-Inf} and the solver
stops there instead.

The back-substitution is written \code{(10^(x_l10))}, not \code{exp10(x_l10)}, although
the latter reads better and works inside a \code{\link[=P]{P()}} transformation: \code{exp10} is a
C99 function with no entry in R's derivatives table, so a model built on it has
no symbolic Jacobian and \code{\link[=Xs]{Xs()}} cannot generate sensitivities. The parentheses
are load-bearing -- R's \code{^} is right-associative, so a bare \code{10^x_l10}
substituted into \code{x^2} would mean \code{10^(x_l10^2)}.

Read results back in R with \code{10^x_l10}.
}
\examples{
log10Transform(eqnvec(x = "-k*x"))
log10Transform(eqnvec(A = "-k1*A + k2*B", B = "k1*A - k2*B"))
}
\seealso{
\code{\link[=odemodel]{odemodel()}}, which compiles the result.
}
