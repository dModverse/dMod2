% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/EM.R
\name{sparsify}
\alias{sparsify}
\title{Structural model selection over the random-effect support}
\usage{
sparsify(
  obj,
  center,
  fixed = NULL,
  method = NULL,
  control = list(),
  fits = NULL,
  cores = 1L,
  sd = 0.5,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{obj}{Composed objective for the full candidate set, carrying a
\link{penaltyL1} specification (via \link{constraintL1}).}

\item{center}{Named numeric start (see \link{emInit}).}

\item{fixed}{Optional named numeric held fixed.}

\item{method}{Backend for the support path (\code{"focei"} default, or \code{"saem"});
ignored for the clustered path.}

\item{control}{List forwarded to the backend.}

\item{fits}{Number of structural multistarts (default \code{12} for the support
path, \code{1} for the clustered path).}

\item{cores}{Number of parallel workers.}

\item{sd}{Perturbation SD for the multistart.}

\item{verbose}{Logical; print the selection.}

\item{...}{Clustered-path options (\code{quadNodes}, \code{quadRule}, \code{level}, \code{lamGrid},
\code{refine}, \code{joint}); see the clustered backend.}
}
\value{
An object of class \code{c("sparsify", "list")}; \code{type = "support"} for the
nested-support path (\code{support}, \code{ranking}, \code{chain}, \code{fitSelected}) or
\code{type = "grouping"} for the clustered path (\code{perParam}, \code{structural},
\code{etaModes}), with a \link[=print.sparsify]{print} method (and, for a grouping,
\link[=plot.sparsify]{plot}).
}
\description{
Cheap marginal-likelihood selection of which parameters are individual-specific
(and, for a clustered penalty, how the individuals group), dispatching on the
\link{penaltyL1} method attached to \code{obj}:
\describe{
\item{\code{"lasso"} / \code{"fused"}}{Forward nested-support selection: one
mixed-effects fit ranks the candidates by deviation magnitude, then an O(K)
nested chain of supports is compared by marginal \eqn{-2\log L}. Returns the
selected \code{support}.}
\item{\code{"clustered"}}{Grouping selection: one all-separate fit provides the
FOCE linearisation, then the clustered marginal scores the cluster path and
selects the grouping per candidate parameter (jointly by default). Returns
the recovered \code{perParam} grouping and a \link[=plot.sparsify]{plot} method.}
}
The marginal integration supplies the Occam factor that a classical penalty
scan needs a lambda grid plus an information criterion to approximate.
}
\seealso{
\link{EM}, \link{penaltyL1}, \link{constraintL1}
}
