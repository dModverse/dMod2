% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/penaltyClass.R
\name{penaltyL1}
\alias{penaltyL1}
\title{L1 penalty for a mixed-effects fit}
\usage{
penaltyL1(
  pars,
  method = c("lasso", "fused", "clustered"),
  target = 0,
  perParam = FALSE,
  subjects = NULL
)
}
\arguments{
\item{pars}{Character vector of base parameter names that are candidates for
being individual-specific, e.g. \code{c("Ka", "Cl")}. Their per-subject
deviations \verb{eta_<par>_<subject>} are penalised.}

\item{method}{One of \code{"lasso"} (element-wise, default), \code{"fused"} (star
toward a shared centre), or \code{"clustered"} (pairwise, groups emerge).}

\item{target}{Scalar or named numeric giving the reference value each
deviation is pulled toward. Default \code{0}. Used only for \code{method = "lasso"}.}

\item{perParam}{Logical; if \code{TRUE}, give each parameter its own penalty
strength \verb{lambda_<par>} (estimated separately by marginal ML: the route to
soft sparsity in the fit, where shared parameters acquire a large strength
that shrinks their deviations, individual ones a small strength). Default
\code{FALSE} (a single global strength \code{lambda}). Not supported for
\code{method = "clustered"}.}

\item{subjects}{Optional character vector of subject identifiers. Required
before the spec is fed to \link{EM}; optional standalone.}
}
\value{
A \code{penaltySpec} (S3 list) with components \code{blocks} (the penalty
blocks), \code{params}, \code{eta}, \code{short}, \code{K}, \code{subjects}, \code{subjectEtas},
\code{lambdaName} (distinct strength names), \code{lambdaByParam}, \code{prefix}.
}
\description{
Declares an L1/Laplace penalty on the per-subject deviations
\verb{eta_<par>_<subject>} of the named parameters: the sparsity/clustering
counterpart of \link{omega}, which declares a Gaussian random effect. In the
mixed-effects reading the deviations are random effects drawn from a Laplace
density whose single strength \code{lambda} is not fixed but ESTIMATED by marginal
maximum likelihood (one \link{EM} fit, no penalty-strength scan). A deviation
driven to zero says the subject shares the population value of that parameter,
so the estimated support of the deviations is the parsimonious "which
parameters are individual-specific" model. Consumed by \link{constraintL1} (which
stamps the spec onto a \code{normL2} objective) and by \link{EM}; combine several
penalty terms with \code{+}, and turn the fit into a hard structural model with
\link{sparsify} (the marginal likelihood supplies the Occam factor a classical
penalty scan needs a lambda grid plus an information criterion to mimic).

The \code{method} chooses which relationship between subjects is penalised, i.e.
which question the fit answers. All three are members of the (generalised)
fused-lasso family:
\describe{
\item{\code{"lasso"} (default)}{Element-wise L1 of each deviation toward a fixed
\code{target} (default 0), \eqn{\lambda \sum_i |\eta_i - t|}. The plain lasso:
it shrinks individual deviations to the target and answers "is this
subject different from the reference value?". It factorises over subjects
(the cheap, per-subject path) and also drives the classical two-group
reference encoding, where one subject is the baseline (no deviation) and
the other carries the single penalised difference. Works for any number of
subjects.}
\item{\code{"fused"}}{Star penalty pulling every deviation toward one shared,
estimated centre (equivalently \code{eta} toward 0, with the centre absorbed
into the structural mean). It answers "is this subject an outlier from the
population mean?", the direct L1 analog of a Gaussian random effect. It
factorises, but needs every subject to carry an anchored deviation (no
reference baseline).}
\item{\code{"clustered"}}{Complete-graph pairwise penalty
\eqn{\sum_{i<j} |\eta_i - \eta_j|}. It answers the richer "how do the
subjects group?": individuals crystallise into an unknown NUMBER of
clusters that share a value, with all-shared (one cluster) and
all-distinct (n clusters) as the two extremes, so the shared vs individual
question is just its two-cluster special case. It COUPLES all
subjects (the penalty is not separable), needs every subject to carry an
anchored deviation, is meaningful from three subjects up, and is scored by
the exact clustered marginal in \link{sparsify}. Higher cost than the
factorising methods.}
}
}
\examples{
pen <- penaltyL1(c("Ka", "Cl"), subjects = paste0("s", 1:3))
pen$eta
gp <- penaltyL1(c("kon", "koff"), method = "clustered",
                subjects = paste0("s", 1:4))
gp$blocks[[1]]$kind
}
\seealso{
\link{constraintL1}, \link{EM}, \link{sparsify}, \link{omega}
}
