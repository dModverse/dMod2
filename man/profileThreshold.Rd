% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/statistics.R
\name{profileThreshold}
\alias{profileThreshold}
\title{Threshold for a profile likelihood}
\usage{
profileThreshold(level = 0.95, method = c("chisq", "F"), n = NULL, p = NULL)
}
\arguments{
\item{level}{the confidence level required.}

\item{method}{\code{"chisq"} (default) or \code{"F"}.}

\item{n}{number of data points. Only for \code{method = "F"}.}

\item{p}{number of estimated mean parameters, that is without the
error-model parameters. Only for \code{method = "F"}. \link{remlLeverage} reports the
effective count, which is what to pass when directions are non-identifiable.}
}
\value{
A single number.
}
\description{
How far a profile has to rise for a parameter value to leave the confidence
region. \code{"chisq"} is the asymptotic \eqn{\chi^2_1} quantile. \code{"F"} is the
finite-sample threshold \eqn{n\log(1 + F_{1,\nu,1-\alpha}/\nu)},
\eqn{\nu = n - p}, exact for Gaussian errors with a single sigma profiled
out and the generalisation of the classical t interval.
}
\seealso{
\link{profile} takes it as \code{delta}, \link{confint.parframe} builds it from
\code{method}, \code{n} and \code{p}. Passing the same one to both keeps the profile long
enough for the interval to be read off rather than extrapolated.
}
